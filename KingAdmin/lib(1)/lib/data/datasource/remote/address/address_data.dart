import 'package:adminkingfashion/core/class/curd.dart';

class AddressDataRemote {
  Crud crud;
  AddressDataRemote(this.crud);

  // addData(String? usersid,String? itemsid)async{
  //    var response = await crud.postData(AppLink.cartadd,{"usersid":usersid,"itemsid":itemsid});
  //    return response.fold((l) => l, (r) => r);
  // }

  // deleteData(String? usersid,String? itemsid)async{
  //    var response = await crud.postData(AppLink.cartdelete,{"usersid":usersid,"itemsid":itemsid});
  //    return response.fold((l) => l, (r) => r);
  // }
  // getCountItemsData(String? usersid,String? itemsid)async{
  //    var response = await crud.postData(AppLink.cartgetcountitems,{"usersid":usersid,"itemsid":itemsid});
  //    return response.fold((l) => l, (r) => r);
  // }

  // viewaddressData(String? usersid)async{
  //    var response = await crud.postData(AppLink.addressView,{"usersid":usersid});
  //    return response.fold((l) => l, (r) => r);
  // }

  // checkCoupon(String couponName)async{
  //   var response = await crud.postData(AppLink.cheackcoupon,{
  //     "couponname":couponName});
  //    return response.fold((l) => l, (r) => r);
  // }
}
